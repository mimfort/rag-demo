# HTTP-клиент для запросов к LM Studio (поддерживает SSE streaming).
httpx>=0.27,<1.0

# Драйвер PostgreSQL. psycopg v3 (не путать с psycopg2) —
# современная версия, по умолчанию ставит [binary] вариант с готовыми C-биндингами.
psycopg[binary]>=3.2,<4.0

# Регистрирует тип `vector` в psycopg, чтобы numpy-массивы / списки автоматически
# конвертировались в pgvector-формат при INSERT и обратно при SELECT.
pgvector>=0.3,<1.0

# numpy используется только в учебной вставке в README (показать как
# cosine similarity считается руками). В рабочем коде поиск делает Postgres.
numpy>=2.0,<3.0

# Читает переменные из .env в os.environ.
python-dotenv>=1.0,<2.0

# --- Веб-интерфейс (web/server.py) ---
# FastAPI — асинхронный веб-фреймворк, в нашем случае нужен ради
# StreamingResponse для Server-Sent Events.
fastapi>=0.115,<1.0

# ASGI-сервер для запуска FastAPI. [standard] — это набор расширений
# (websockets, httptools, watchfiles) — не обязательны, но удобны.
uvicorn[standard]>=0.30,<1.0
